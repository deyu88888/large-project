import React from "react";
interface HoverCardProps {
    userId: number;
    children: React.ReactNode;
}
export declare function HoverCard({ userId, children }: HoverCardProps): import("react/jsx-runtime").JSX.Element;
export {};
