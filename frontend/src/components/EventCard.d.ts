import { EventCardProps } from "../types/shared/event";
declare const EventCard: ({ event, isLight, colors, onViewEvent, followingsAttending, }: EventCardProps) => import("react/jsx-runtime").JSX.Element;
export default EventCard;
