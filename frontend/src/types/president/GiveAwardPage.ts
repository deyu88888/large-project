export interface AwardAssignmentPayload {
  student_id: number;
  award_id: number;
}

export interface ThemeColors {
  backgroundColor: string;
  textColor: string;
  subtitleColor: string;
  paperBackgroundColor: string;
}